import React from 'react';


//деструктуризация параметров прямо в аргументах функции
const Logout = ({ handleLogout }) => <button
onClick={this.handleLogout}
  className="btn logout">Logout</button>
export default Logout;